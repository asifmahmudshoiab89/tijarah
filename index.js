const express=require('express');const app=express();app.use(express.static('public'));app.get('/api/health',(req,res)=>res.json({ok:true,name:'Tijarah'}));app.get('/api/products',(req,res)=>res.json([{name:'Keyboard',price:900,stock:18},{name:'Mouse',price:550,stock:42}]));app.get('/api/services',(req,res)=>res.json([{name:'1000 Coins',cost:1600,price:1900}]));app.get('/api/settings',(req,res)=>res.json({business_name:'Tijarah',currency:'BDT',charge_per_1000:'13.95'}));app.get('/api/calculator/charge',(req,res)=>{const a=Number(req.query.amount),r=Number(req.query.rate);const charge=a*r/1000;res.json({charge,total:a+charge})});app.listen(process.env.PORT||3000);